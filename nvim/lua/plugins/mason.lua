return {
    
    {
        "williamboman/mason.nvim",
        opts = {
        --    ensure_installed = {
        --        "clangd",
        --        "clang-format",
        --        "lua-language-server",
        --    }
        },
        --config = function(_, opts)
        --    require("mason").setup(opts)
        --end,
        config = true,
    },

}

